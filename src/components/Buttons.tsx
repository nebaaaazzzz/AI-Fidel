export function YellowishButtons({ text }) {
  return <button className={`btn capitalize px-20 rounded-xl bg-accent`}>{text}</button>;
}
export function GreenishButtons({ text }) {
  return <button className={`btn capitalize px-20 rounded-xl bg-primary`}>{text}</button>;
}
